package com.ohimai.backend;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

/**
 * Entry point utama aplikasi OHIM AI Backend.
 * Menjalankan Spring Boot application context.
 */
@SpringBootApplication
public class OhimAiBackendApplication {

    public static void main(String[] args) {
        SpringApplication.run(OhimAiBackendApplication.class, args);
    }
}
